// Mock data placeholder — replace with real data/API calls
export const mockCountries: { id: string; name: string; flag: string }[] = [];

export const mockPrayerRequests: { id: string; title: string; body: string; country: string; createdAt: string }[] = [];
